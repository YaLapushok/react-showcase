# Используем официальный образ Node.js (Alpine - легковесная версия)
FROM node:18-alpine

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем наш скрипт симулятора в рабочую директорию
COPY mock-device-server.js .

# Открываем порт, который слушает наш симулятор (для информации, внутри docker-compose это не обязательно)
EXPOSE 6000

# Команда для запуска симулятора при старте контейнера
CMD [ "node", "mock-device-server.js" ] 